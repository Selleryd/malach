function setBilling(mode){document.querySelectorAll('[data-billing]').forEach(btn=>btn.classList.toggle('active',btn.dataset.billing===mode));document.querySelectorAll('[data-plan-card]').forEach(card=>{card.querySelectorAll('[data-monthly]').forEach(el=>el.hidden=mode!=='monthly');card.querySelectorAll('[data-annual]').forEach(el=>el.hidden=mode!=='annual');const cta=card.querySelector('[data-plan-cta]');if(cta)cta.href=`https://app.malach.app/?plan=${encodeURIComponent(card.dataset.plan)}&billing=${mode}`})}
document.querySelectorAll('[data-billing]').forEach(btn=>btn.addEventListener('click',()=>setBilling(btn.dataset.billing)));setBilling('monthly');
